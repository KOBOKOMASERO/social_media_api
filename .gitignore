# Python/Django
__pycache__/
*.py[cod]
*.sqlite3
db.sqlite3
venv/
env/
.venv/
*.pyo
*.pyd

# Django migrations (optional, usually we keep them)
*/migrations/*.py[cod]
*/migrations/__pycache__/

# OS files
.DS_Store
*.log

# Node
node_modules/
package-lock.json
npm-debug.log
yarn.lock

# IDEs
.vscode/
.idea/
# Project-level files
README.md
requirements.txt
runtime.txt (if on Railway/Heroku)
Procfile (if deploying)
.env.example (not the real .env)

# Django apps
accounts/
posts/
notifications/
social_media_api/  # your main project folder with settings.py, urls.py, wsgi.py, etc.

# Only track migrations if you want (optional)
*/migrations/*.py   # if you want initial migrations in git
*/migrations/__init__.py
